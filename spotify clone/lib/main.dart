import 'package:flutter/material.dart';
import 'package:spotify_app/screens/app.dart';

void main() {
  runApp(MaterialApp(
      title: 'Spotify clone',
      home:
          new MyApp() // to connect with MyApp.MyApp is object so we may write or skip new
      ));
}
